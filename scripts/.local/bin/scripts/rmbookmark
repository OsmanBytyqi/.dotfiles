#!/bin/sh

path=~/.local/share/larbs/snippets
chosen=$(cut -d ';' -f1 "$path" | dmenu -i -l 20 | sed "s/ .*//")

if [ -n "$chosen" ] ; then
    sed -i -e "/$chosen/d" "$path"
else
    notify-send "not deleted"
    exit
fi

